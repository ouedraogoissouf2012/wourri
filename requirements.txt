# WOURI - FastAPI Backend
# Installation: pip install -r requirements.txt

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
# Sprint I.c.1 review : ajout slowapi (utilise par app/security.py + app/main.py
# pour rate limiting, mais absent du requirements — dette pre-existante
# revelee par la nouvelle CI pytest qui s'auto-declenche sur la PR).
slowapi==0.1.9
# Sprint I.c.1 review : ajout rapidfuzz (utilise par app/services/asr_normalizer.py
# pour fuzzy matching ASR — meme cause root que slowapi : dette pre-existante).
rapidfuzz==3.14.3

# Templates
jinja2==3.1.3
aiofiles==23.2.1

# HTTP Client
httpx==0.26.0

# Validation
pydantic==2.5.3
pydantic-settings==2.1.0

# TTS francais — Piper (voix locale fr_FR-tom-medium). Le CLI `piper` est
# fourni par piper-tts ; le modele est telecharge dans l'image (Dockerfile.prod)
# et pointe par PIPER_MODEL_FR au runtime. edge-tts retire : dependance morte,
# jamais importee dans le code (cf. docs/AUDIT_DIOULA_2026-08.md).
piper-tts==1.6.1

# ML/AI (Hugging Face)
transformers>=4.36.0
torch>=2.1.0
scipy>=1.11.4
soundfile>=0.12.1
# faster-whisper : STT francais (app/services/stt_whisper.py:161). Meme cause
# root que slowapi/rapidfuzz : importe au runtime mais absent du requirements
# (dette revelee par la reinstall Python 2026-07 — issue #313). Pin 1.2.1 =
# version validee localement (compatible ctranslate2 4.8.1 + numpy 2.4).
faster-whisper==1.2.1
# librosa : chargement audio du provider ASR MMS-dyu (mms_dyu_provider.py:62).
# Même dette que faster-whisper : importé au runtime, absent du requirements
# (révélé par l'audit dioula #358). Pin = version validée localement.
librosa==0.11.0

# Audio conversion
pydub>=0.25.1

# Utilities
python-dotenv==1.0.0
# psutil : observabilité mémoire (utilisé par /health pour rapporter RSS/VMS
# du process Python — ADR-0011 Phase 4) et par tools/profile_preload.py
psutil>=5.9.0

# ========== BD Vectorielle IVR : PostgreSQL + pgvector (ADR-0008) ==========
# chromadb RETIRÉ (#203, Phase E terminée) : incompatible numpy 2.x
# (np.float_ supprimé en NumPy 2.0) — pgvector est le backend unique.
sqlalchemy==2.0.49
alembic==1.18.4
psycopg[binary]==3.3.4
pgvector==0.4.2
# sentence-transformers : embeddings du corpus (corpus_service + import).
sentence-transformers>=2.2.0
