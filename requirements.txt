# WOURI - FastAPI Backend
# Installation: pip install -r requirements.txt

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
# Sprint I.c.1 review : ajout slowapi (utilise par app/security.py + app/main.py
# pour rate limiting, mais absent du requirements — dette pre-existante
# revelee par la nouvelle CI pytest qui s'auto-declenche sur la PR).
slowapi==0.1.9

# Templates
jinja2==3.1.3
aiofiles==23.2.1

# HTTP Client
httpx==0.26.0

# Validation
pydantic==2.5.3
pydantic-settings==2.1.0

# TTS
edge-tts==6.1.9

# ML/AI (Hugging Face)
transformers>=4.36.0
torch>=2.1.0
scipy>=1.11.4
soundfile>=0.12.1

# Audio conversion
pydub>=0.25.1

# Utilities
python-dotenv==1.0.0
# psutil : observabilité mémoire (utilisé par /health pour rapporter RSS/VMS
# du process Python — ADR-0011 Phase 4) et par tools/profile_preload.py
psutil>=5.9.0

# BD Vectorielle IVR
chromadb>=0.4.0

# ========== PostgreSQL + pgvector (Sprint F Phase B — ADR-0008) ==========
# Migration progressive ChromaDB -> Postgres+pgvector :
# - Phase B (en cours) : schema + migrations + import corpus (code NON activé en prod)
# - Phase C : adapter + double-écriture (feature flag)
# - Phase D : validation terrain
# - Phase E : bascule + dépréciation chromadb
sqlalchemy==2.0.49
alembic==1.18.4
psycopg[binary]==3.3.4
pgvector==0.4.2
# sentence-transformers : déjà tiré par chromadb mais on l'épingle explicitement
# car scripts/import_corpus_ivr.py l'importe directement (pas via vdb_service)
sentence-transformers>=2.2.0
