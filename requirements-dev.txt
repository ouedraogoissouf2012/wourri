# WOURI - Dépendances de développement (tests, lint, etc.)
#
# Install : pip install -r requirements-dev.txt
#
# Distinct de requirements.txt (production) pour ne pas alourdir le runtime.

pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=7.1.0,<8.0
